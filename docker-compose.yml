version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # DATABASE_URL must be provided in localized .env file or injected
      - PORT=3000
    env_file:
      - .env
    restart: always
